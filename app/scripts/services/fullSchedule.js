'use strict';

app.factory('FullSchedule', function ($q) {

  // let fullSchedule;

  // let getFullSchedule = () => {
  //   return $q( (resolve, reject) => {
  //     firebase.database().ref('/Sessions').on('value', function(session) {
  //       fullSchedule = session.val()
  //       resolve(fullSchedule)
  //     });

  //   })
  // }

  // return { getFullSchedule };

});
